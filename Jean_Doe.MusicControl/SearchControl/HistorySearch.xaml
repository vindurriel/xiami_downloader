<ComboBox x:Class="Jean_Doe.MusicControl.HistorySearch"
          TextSearch.TextPath="Key"
          IsEditable="True"
          xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
          xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ComboBox.Style>
        <Style   TargetType="ComboBox" BasedOn="{StaticResource {x:Type ComboBox}}"/>
    </ComboBox.Style>
    <ComboBox.ItemTemplate>
        <DataTemplate>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                    <ColumnDefinition Width="auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="{Binding Key}" VerticalAlignment="Center" TextTrimming="WordEllipsis"/>
                <TextBlock Grid.Column="1" Margin="5,0,0,0"  Text="{Binding SearchCount,StringFormat={}搜索{0}次}" />
                <FrameworkElement Grid.Column="2"  Width="{DynamicResource {x:Static SystemParameters.VerticalScrollBarWidthKey}}" />
            </Grid>
        </DataTemplate>
    </ComboBox.ItemTemplate>
</ComboBox>
